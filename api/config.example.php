<?php
/**
 * Настройки отправки заявок.
 * 1) Скопируйте этот файл в config.php (он НЕ попадает в git).
 * 2) Заполните нужные каналы. Пустой канал просто пропускается.
 * Рекомендуется держать включёнными минимум два канала (например, мессенджер + почта).
 */
return [
    // ---------- Telegram ----------
    // Токен выдаёт @BotFather. chat_id — id ответственного или группы (см. docs/SETUP.md).
    'telegram' => [
        'enabled'   => false,
        'bot_token' => '',          // 123456789:AA...
        'chat_id'   => '',          // 123456789 или -100123... для группы
    ],

    // ---------- MAX ----------
    // Бот создаётся на business.max.ru после верификации ИП/организации/самозанятого.
    // Личный диалог адресуется по user_id, групповой чат — по chat_id.
    'max' => [
        'enabled'   => false,
        'api_base'  => 'https://platform-api2.max.ru',
        'bot_token' => '',
        'user_id'   => '',          // заполните ЛИБО user_id…
        'chat_id'   => '',          // …ЛИБО chat_id группового чата
        // Домен platform-api2.max.ru использует сертификат Минцифры.
        // Укажите путь к файлу корневого сертификата (.pem/.crt), если хостинг его не знает.
        'ca_file'   => '',
    ],

    // ---------- E-mail (резервный канал) ----------
    'email' => [
        'enabled' => true,
        'to'      => 'svkondratovaum@yandex.ru',
        'from'    => 'no-reply@ВАШ-ДОМЕН.ru',   // адрес на домене сайта, иначе письма уйдут в спам
    ],

    // ---------- Прочее ----------
    // Домены, с которых разрешено отправлять форму (защита от чужих сайтов).
    'allowed_origins' => ['https://ВАШ-ДОМЕН.ru', 'https://www.ВАШ-ДОМЕН.ru'],
    // Резервная копия заявок в файл (на случай сбоя всех каналов). Папка должна быть закрыта от веба.
    'log_file' => __DIR__ . '/../storage/leads.log',
];
